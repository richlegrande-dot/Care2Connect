# Frontend environment variables — local development
# Copy to .env.local and fill in real values.
# This file is committed to git (it contains only placeholders, no secrets).
# .env.local is gitignored and must NEVER be committed.

# ─── API URLs ────────────────────────────────────────────────────────────────

# External-facing API URL (used by browser requests via rewrite proxy)
NEXT_PUBLIC_API_URL=https://api.care2connects.org/api

# BACKEND_INTERNAL_URL is server-side only (no NEXT_PUBLIC_ prefix).
# It is used by App Router API routes (like /papi/*) to talk to the backend
# directly over localhost, bypassing Cloudflare/Caddy.
# Set this to http://localhost:3001 for local dev.
# In production containers this would be the k8s service address.
BACKEND_INTERNAL_URL=http://localhost:3001

# Public frontend base URL (used for absolute links, CORS)
NEXT_PUBLIC_FRONTEND_URL=https://care2connects.org

# ─── Feature Flags ───────────────────────────────────────────────────────────

# REQUIRED: Gate for the V2 intake wizard. 
# Without this set to "true", /onboarding/v2 silently redirects to /.
NEXT_PUBLIC_ENABLE_V2_INTAKE=true

# ─── Provider Dashboard ──────────────────────────────────────────────────────

# Pre-filled token for the provider login page.
# DO NOT commit real value — fill in below for your local environment.
NEXT_PUBLIC_PROVIDER_DASHBOARD_TOKEN=__SET_ME__
