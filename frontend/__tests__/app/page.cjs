// Auto-generated shim for tests
module.exports = require('../../app/page')

// Prevent Jest from failing if this shim is collected as a test file
if (typeof test === 'function') {
  test.skip('shim: app/page placeholder', () => {})
}
