'use client'\n\nimport { useState } from 'react'\nimport { useRouter } from 'next/navigation'\nimport Link from 'next/link'\n\nexport default function LocationPage() {\n  const router = useRouter()\n  const [country, setCountry] = useState('United States')\n  const [zipCode, setZipCode] = useState('')\n\n  const handleNext = () => {\n    if (zipCode.length >= 5) {\n      // Store data in localStorage for V1 simplicity\n      localStorage.setItem('gfm_location', JSON.stringify({ country, zipCode }))\n      router.push('/gfm/2-category')\n    }\n  }\n\n  return (\n    <div className=\"max-w-2xl mx-auto\">\n      {/* Progress Bar */}\n      <div className=\"mb-8\">\n        <div className=\"flex items-center justify-between text-sm text-gray-500 mb-2\">\n          <span>Step 1 of 7</span>\n          <span>Location</span>\n        </div>\n        <div className=\"w-full bg-gray-200 rounded-full h-2\">\n          <div className=\"bg-blue-600 h-2 rounded-full\" style={{width: '14.3%'}}></div>\n        </div>\n      </div>\n\n      <div className=\"card\">\n        <h1 className=\"text-2xl font-bold text-gray-900 mb-6\">\n          Where will the funds go?\n        </h1>\n        <p className=\"text-gray-600 mb-8\">\n          Tell us where you're located so we can show your fundraiser to people nearby.\n        </p>\n\n        <div className=\"space-y-6\">\n          {/* Country Selection */}\n          <div>\n            <label className=\"block text-sm font-medium text-gray-700 mb-2\">\n              Country *\n            </label>\n            <select \n              value={country}\n              onChange={(e) => setCountry(e.target.value)}\n              className=\"form-input\"\n            >\n              <option value=\"United States\">United States</option>\n              <option value=\"Canada\">Canada</option>\n              <option value=\"United Kingdom\">United Kingdom</option>\n              <option value=\"Australia\">Australia</option>\n            </select>\n          </div>\n\n          {/* ZIP Code */}\n          <div>\n            <label className=\"block text-sm font-medium text-gray-700 mb-2\">\n              ZIP code *\n            </label>\n            <input\n              type=\"text\"\n              value={zipCode}\n              onChange={(e) => setZipCode(e.target.value.replace(/\\D/g, '').slice(0, 5))}\n              placeholder=\"Enter your ZIP code\"\n              className=\"form-input\"\n              maxLength={5}\n            />\n            <p className=\"text-sm text-gray-500 mt-2\">\n              Your ZIP code helps us show your fundraiser to people in your area.\n            </p>\n          </div>\n        </div>\n\n        {/* Navigation */}\n        <div className=\"flex justify-between items-center mt-8 pt-6 border-t\">\n          <Link href=\"/\" className=\"btn-secondary\">\n            ← Back to Home\n          </Link>\n          \n          <button \n            onClick={handleNext}\n            disabled={zipCode.length < 5}\n            className={`btn-primary ${zipCode.length < 5 ? 'opacity-50 cursor-not-allowed' : ''}`}\n          >\n            Continue →\n          </button>\n        </div>\n      </div>\n    </div>\n  )\n}